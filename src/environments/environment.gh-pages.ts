export const environment = {
  production: true,
  routeBase: '',
  authRoute: 'auth',
  feedRoute: 'feed',
  baseRoute: 'feed',
  postRoute: 'posts/:id',
  postRouteBase: 'posts',
  notFoundRoute: '404',
  debugRouting: false,
};
